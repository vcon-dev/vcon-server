CONSERVER_API_TOKEN=fake-token
DEEPGRAM_KEY=fake-deepgram-key
REDIS_URL=redis://localhost:6379

# LLM API Keys (set these locally in .env.test, do not commit real keys)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=

# Enable integration tests that call real APIs (default off)
RUN_OPENAI_ANALYZE_TESTS=0


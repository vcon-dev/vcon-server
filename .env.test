CONSERVER_API_TOKEN=fake-token
# DEEPGRAM_KEY is intentionally not set here. The deepgram integration tests
# skip when it's absent. To run them locally, export DEEPGRAM_KEY in your shell
# (see docs/configuration/environment-variables.md); in CI, inject via GitHub
# Secrets if a dedicated test key is provisioned.
#
# The previous committed key has been rotated — do not restore it.
